{% extends "base.html.twig" %}

{% block title %}About{% endblock %}

{% block body %}
    <h1>API</h1>

    <h2>Current API-routes:</h2>
    <div class="api-container">

    <p><a class="api-link" href="{{ path('api') }}">/api</a> - This very summation page, listing all the JSON-related routes. </p>

    <p><a class="api-link" href="{{ path('api/quote') }}">/api/quote</a> - JSON-route offering up a random Groucho Marx-quote, the current date and the
    timestamp of the quote-generation. </p>
    <br><br>

    <p><a class="api-link2" href="{{ path('api/deck') }}">/api/deck</a> - JSON-route showcasing new, ordered decks, including and excluding jokers, and misc details.</p>

    <p><a class="api-link2" href="{{ path('api/deck/get/shuffle') }}">/api/deck/get/shuffle</a> - JSON-route shuffling, and then showcasing and saving to session, a new 52-card deck, excluding jokers.</p>

    <p><a class="api-link2" href="{{ path('api/deck/get/shuffle') }}">/api/deck/get/shuffle</a> - JSON-route shuffling, and then showcasing and saving to session, a new 52-card deck, excluding jokers.</p>

    <p><a class="api-link2" href="{{ path('api/quote') }}">/api/quote</a> - JSON-route offering up a random Groucho Marx-quote, the current date and the
    timestamp of the quote-generation. </p>
</div>
{% endblock %}